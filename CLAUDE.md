# 코레일플러스 UI 재현 프로토타입

## 이 프로젝트가 뭔가

실제 코레일플러스 앱(`com.korail.talk`) 화면을 **계측 기반으로 픽셀 단위 재현**하고,
그 위에 신규 기능 두 개의 삽입 지점을 제안하는 프로토타입.

목적은 두 가지다.
1. 심사자가 실제 UX 플로우를 직접 체험하게 하는 것
2. "미니게임은 홈의 여기, 탄소리포트는 마이페이지의 여기가 적절하다"는 주장을 눈으로 보여주는 것

따라서 **원본과 구분이 안 될수록 목적에 부합한다.** 임의로 디자인을 개선하거나
"더 나아 보이는" 값으로 바꾸지 말 것. 값은 전부 실측에서 나왔다.

## 신규 제안 요소

| 기능 | 위치 | 근거 |
|---|---|---|
| 미니게임 (레일런) | 홈 → 서비스 그리드 3행 4열 | 기존 11개 + 1 = 4×3 격자를 정확히 채움. 새 공간 요구 아님 |
| 탄소리포트 | 마이페이지 → 계정 관리와 마일리지 사이 | 계정 정보 다음, 리워드 앞 = "내가 만든 결과" |

## 계측 기준 (절대 바꾸지 말 것)

```
기기      갤럭시 S25+
해상도    1080 × 2340 px
density   450  →  배율 2.8125
논리 크기  384 × 832 dp
CSS px = dp = sp  (1:1)
```

좌표는 `adb shell uiautomator dump` 실측값 ÷ 2.8125.
글꼴 크기는 스크린샷의 글자 잉크 폭·높이를 실제 폰트 렌더링과 대조해 역산.
글자 좌표는 `잉크 x0 − 사이드베어링`, `잉크 y0 − 하프리딩 보정`.
색상은 스크린샷 픽셀 직접 추출.

**소수점 값(19.91, 344.18, 13.87 등)은 오타가 아니다.** 정수로 반올림하면 원본과 어긋난다.

## 파일 구조

```
build/
├ index.html      홈 (전체 스크롤)
├ allmenu.html    전체메뉴 (전체 스크롤)
├ mypage.html     마이페이지 — 탄소리포트 삽입
├ carbon.html     탄소리포트 (자체 스타일, app.css 미사용)
├ game.html       레일런 미니게임 (자체 스타일, canvas)
├ app.css         공통 스타일 — index/allmenu/mypage 가 공유
├ fit.js          기기별 배율 계산
├ img/            스크린샷에서 알파 분리 추출한 에셋 30여 개
└ fonts/          Pretendard GOV woff2 서브셋 (APK res/font 추출)
```

## 핵심 설계

### 반응형 = 유동 배치가 아니라 전체 배율 조정
384dp 레이아웃을 고정하고 `fit.js`가 `화면폭 / 384` 배율로 통째로 스케일한다.
어떤 기기에서도 원본과 **같은 비율**을 유지하는 게 목표이기 때문.

유동 배치(flex가 늘어나는 방식)로 되돌리지 말 것. 이전에 그렇게 했다가
헤더 내용물이 좁은 화면에서 넘쳐 잘리는 문제가 있었다.

### 주요 토큰 (app.css `:root`)
```
--gutter    19.91px   좌우 여백 (측정 266회, 압도적 1위)
--tap       48px      터치 요소 표준 높이 (173회)
--content   344.18px  콘텐츠 폭
--card      #1D1F28   표준 카드 (298회)
--card-alt  #212634   검색 카드 등 밝은 카드
--primary   #2355C9
```

### 시연용 강조
`<body data-demo="on">` → 신규 요소와 클릭 유도 지점에 점멸 테두리.
`"off"` 로 바꾸면 전부 사라진다. 발표에서 토글해 쓰는 용도.

강조 대상: 홈 미니게임 셀, 홈 전체메뉴 탭, 전체메뉴 마이페이지 행, 마이페이지 탄소리포트 섹션.

### 시연 동선
```
홈 → [전체메뉴 탭] → 전체메뉴 → [마이페이지] → 마이페이지 → [탄소리포트] → 탄소리포트
홈 스크롤 → [미니게임] → 레일런
```

## 계측 도구 (프로젝트 루트 밖, korail_kit/korail/tools)

```bash
bash tools/shot.sh <이름>              # 스크린샷 + dump 동시 캡처
python tools/measure.py shots/<이름>   # 좌표·글꼴·색상 자동 측정 → .md / .css 생성
```

`shot.sh` 실행 전 필요:
```bash
export PATH=$PATH:"/c/Users/kyhan/Downloads/platform-tools-latest-windows/platform-tools"
export MSYS_NO_PATHCONV=1   # Git Bash 경로 자동변환 끄기
adb devices                  # device 로 떠야 함
```

## 미구현

- 이동·편의 / 여행 / 나의 티켓 탭 — 시연 동선에 없어 보류. 캡처는 확보됨
  (`move_1~4`, `travel_1~7`, `myticket_1`). `myticket` 은 스크롤 하단 캡처 추가 필요
- `border-radius` — dump·APK 어디에도 없어 추정값 (16px / 12px)

## 작업 시 주의

- 로컬 확인은 반드시 서버로. `file://` 은 웹폰트가 차단된다
  ```bash
  npx serve
  ```
- 저장소는 **Private 유지.** 코레일 로고·아이콘이 포함돼 있다
- 폰트는 상용 한글 2,350자 서브셋. 흔치 않은 글자는 깨질 수 있다
- 배포는 Cloudflare Pages. `main` 에 push 하면 자동 재배포



=========290910 19:01===========
할 일 
1. 탄소리포트 산정근거 확실히 확인 후 넣기
논문까지 실제확인
피그마 이용

2,3,4,5를 먼저 하고 1나중에

2. 피그마 탄소리포트 ui/ux변경 좋은 레퍼런스 참조해서 클로드가 넣은 기본 아이콘들을 직접 디자인해서 넣기
3. 피그마 fluid css적용
홈 <-> 전체 메뉴 화면 전환 등 부드럽게 페인드 인 페이드 아웃 코레일 앱 기존 uiux 적용그대로 넣기
4. 피그마 게임내 소리 on/off 아이콘 수정 직접 피그마 디자인
5. 피그마 게임내 게임 오버시 다시 운행 알림창 등 화면 디자인 ui ux
6. 코레일 앱 초기로딩화면 스샷 찍은 후
이거 0.5초 보여준 후 홈화면 나오게

# hotfix 
1. 게임 진행중 소리켜기 소리끄기 버튼을 클리갷도 열차가 이동하는것으로 인식함. 소리켜기 끄기 버튼은 이동 안하게 수정.



=====260912 15:34=============

# todo
<!-- 1. 게임
 - 순위 리더보드 만들기 -> supabase로 만들기
 - 게임플레이 리더보드 등을 볼 수 있도록 하도록 게임 플레이 버튼, 리더보드 버튼이 같이있는 ui버튼을 새로 구성 -->


# todo after
1. 게임
- 영상광고는 15초 지나면 영상 끌 수 있는 'X표시'든 '광고 사총 종료'
- 위 버튼 클릭시 사용자는 세 가지 선택 하게 됨 (1) 광고 계속 보기 (2)광고 관련된 사이트 이동하기 (3) 게임 돌아가기

2. 광고 리더보드 순위 rule 명확화 브레인스토밍 필요.
- 광고로 계속 살 수 있는건 1번으로 제한 -> 계속할 경우 리더보드 만든 의미가 없어짐.
- 영상 리워드로 도달 KM와 역을 어느정도까지 늘일 수있을지 확실한 RULE 필요.
- 광고 보고 무한으로 늘린다면 리더보드 의미가 없어짐 -> 실력 상관없이 시간 많은 사람이 장떙
- 기회를 주되 리더보드 순위 공정성 위해 게임 순위 유지 기회 제한은 있어야 한다는 의미

2. 탄소리포트
- 탄소리포트 235kg부분이 숫자가 0부터 빠르게 촤르륵 올라가는 효과 구현
- 소나무 36그루는 기치가 지나간 지나가면서 지나간자리에 순서대로 새싹이 자라고 새싹이 소나무로 자라는 애니메이션 효과 구현
- 주간, 월간, 연간에 그래프 그려넣기 그래프도 사용자가 화면을 내렸을 때 그 화면에 다다랐을 때 막대그래프가 올라가는 효과
- 여름캠페인 부분12,847톤 co2부분 -> 녹색 그래프가 올라가는 애니메이션 효과
<!-- - 산정근거 보기 아래
이동편의 저체 메뉴 등 삭제 (완료) -->
- 숫자 올라가는 애니메이션 효과 등 넣은 이후로 
탄소리포트에서 스크롤 내리는 게 바로 실행되지 않고 일정 시간 후에 내릴 수 있음 이 부분 수정

3. 게임 및 탄소리포트
레퍼런스 참조 -> 피그마 디자인 부터 마치기
그 이후에 필요 UI 컴포넌트 디자인 및 애니메이션 효과 넣기.

4. 탄소리포트 


========260917 13:29=======
# 게임
1. share 공유하기 버튼 추가
목적
- 게임 종료 후 리더보드가 뜨면 그걸 친구들에게 자랑하기 용도 카톡 공유하기 기능 추가
구현
- 상대방이 받은 카톡 썸네일에는 공유한 사람의 리더 보드 화면이 나옴

- share icon넣고 카톡으로 공유하기 기능 추가
카톡에 공유되어 보이는 화면 클로드 브레인 스토밍 후 넣기

<!-- 2. 홈화면 <->전체화면 전환 또는 탄소리포트, 게임 접속시 fluid ux로 지금 처럼 끊기는게 아니라 부드럽게 사용자 flow 꺠지 않도록 하는 방법 클로드와 브레인 스토밍

3. 광고를 보던 중 뒤로 가기를 누르면 미니게임이 열린 상태의 홈화면으로 돌아가는데
기본 상태의 홈화면으로 돌아가야 함.

4. 미니게임 아이콘은 일반 갤럭시 폴더처럼 3*3 그리드에 각각 아이콘 크기는 동일해야함.
(현재는 레일런 시작 버튼만 크기가 크다)
이건 피그마로 내가 잡아주기 -->

<!-- 5. 레일런 게임 아이콘 힉스필드, 미드저니로, 클로드 디자인 등으로 아이콘 제작 필요
-> 클로드 브레인 스토밍 먼저 어떤식으로 프롬프트짤지 -->

# 탄소리포트
- 탄소리포트 화면 uiux 구현 레퍼런스 참조
-> 추가할 내용, 수정할 내용, 삭제할 내용부터 확실히하기
레퍼런스 다수 참조할 것.

- 필요한 아이콘 이미지 등은 단순 아이콘 참조나 피그마로 그리기 보다 힉스필드, 미드저니, 클로드 디자인 활용.


# hotfix
<!-- 1. 홈화면 <->전체 화면 페이드인 페이드아웃 방식(이게 원래 코레일 앱 공식 전환 방식임)
전체 화면 -> 탄소리포트 이동시에도 이 방식 적용(현재는 화면이 옆으로 슬라이드되서 이동하는 방식)
탄소리포트 게임에서 뒤로가기 버튼 클릭시 탄소리포트 ->전체메뉴-> 홈화면 이동되지 않고 화면 까맣게만 보이는 현상 발생 게임도 ->홈화면 이동 안됨 확인 필요
원인 및 해결 -->


========260918 15:46=======
# 이번 세션에서 완료한 작업

## game.html
- hotfix: 소리 켜기/끄기 버튼 클릭이 열차 이동으로 오인식되던 문제 수정 (pointerdown에서 #sound 클릭은 제외)
- Supabase 리더보드 추가: 게임오버 시 자동으로 닉네임 생성(형용사+명사, 중복이면 번호 접미사) → players/runs insert →
  상위 20위 리더보드 표시(내 기록 하이라이트, 20위 밖이면 "내 순위: N위") → 닉네임 인라인 변경(🎲 다시뽑기 / 확인)
- 리워드 광고 개편:
  - 광고 선택 완전 랜덤화(직전 영상 제외), ADS 4개 추가, CTA는 url(외부링크)/internalLink(내부이동)/없음 3분기
  - 재생 실패(임베드 차단 등) 시 최대 2회 자동 재시도, 그래도 실패하면 "다른 영상을 준비 중이에요" + 보상은 정상 지급
  - 보상 조건 변경: 15초 미만 영상은 끝까지 재생 시, 15초 이상은 누적 시청 15초 도달 즉시 지급(영상 계속 재생, 토스트 +
    [계속 보기]/[게임으로 돌아가기] 노출). X 버튼은 보상 전엔 확인창, 보상 후엔 즉시 닫힘
- 버그 수정: "운행 시작"이 기회(chances) 차감 없이 새로고침만 하면 무제한 플레이되던 문제 →
  시작/재도전을 attemptStart() 하나로 통합, 기회 0일 때 페이지 로드 시점에도 곧장 광고 유도 화면으로 전환

## carbon.html
- 숫자 카운트업(235kg, 12,847톤) easeInOutCubic + 완료 시 바운스(scale 1→1.08→1)
- 상단 진행바 0→68% 애니메이션, 주/월/연 막대그래프 스크롤 진입 시 애니메이션(IntersectionObserver, 탭 전환마다 재생)
- prefers-reduced-motion 대응

## index.html — 미니게임 진입 UX
- 미니게임 진입을 "페이지 이동" → "갤럭시 앱 폴더 스타일 인앱 오버레이"로 전면 교체
  (minigame-folder.html 을 먼저 만들었다가 이 방식으로 대체 — 더 이상 링크되지 않는 고아 페이지로 남아 있음, 필요시 삭제 요청 바람)
- 클릭한 아이콘 위치에서 확대되며 열리는 애니메이션(transform-origin 계산), 뒤로가기(pushState/popstate)로 자연스럽게 닫힘
- 레일런 카드 아이콘을 업로드받은 img/ic_railrun.png 로 교체
- 폴더 그리드를 3열 × 동일 크기 정사각형 셀로 통일 (레일런 카드만 크게 표시되던 문제 수정 — 정확한 실측값은 Figma 나오는 대로 추가 반영 필요)
- 버그 수정: 게임 진입 → 광고 오버레이 → 뒤로가기 시 "폴더 열린 홈화면"으로 돌아가던 문제 →
  pagehide 에서 폴더/모달을 닫힌 기본 상태로 되돌려 bfcache 스냅샷이 깨끗하게 저장되도록 수정

## 전체 페이지 전환 애니메이션 (transition.js 신규)
- 내부 링크 클릭을 가로채 페이드 아웃 후 이동, sessionStorage 로 다음 페이지에 전달해 pageshow 시 역재생
- 처음엔 fade/slide-in/slide-out 세 종류였다가 hotfix로 전부 fade 하나로 통일 (슬라이드 관련 CSS 제거)
- bfcache 뒤로가기 시 화면이 검게 남는 버그 수정: pagehide 에서 leave 클래스 제거 + pageshow 는 persisted 여부와
  무관하게 정리 + 0.3초 fallback 으로 무조건 화면 복구

## 막힌 것 / 다음에 필요한 것
- "미니게임 폴더에 Figma 실측값 적용" 요청 — 이 세션엔 Figma MCP 가 연결돼 있지 않아 보류됨.
  Figma Dev Mode MCP(`claude mcp add --transport http figma-dev-mode-mcp-server http://127.0.0.1:3845/mcp`)
  또는 community `figma-developer-mcp`(API 키 방식) 연결 후 새 세션에서 재요청 필요







  ========260919 00:14=========
  게임
  ========260919 00:14=========
  게임
  1. 내가 생각한 부스터는 지금처럼 여러번 성공시
  화면 효과 변경만 되는 게 아님
  -> 특정 시간안에 예를들어 30초안에 30번 입력하면
  부스터 동작 ->열차가 자동으로 빠르게 움직임
  (플레이어 리스크 감수 목적, 게임 재미 높이는 목적)
  (열차가 몇초 안에 몇 번 입력해야 빠르게 움직일지는, 또 이걸 반복적으로 계속 사용못하게 할지 아님 허요할 지, 부스터 지속시간은 얼마나 될지는 클로드와 브레인 스토밍)

  2. 배경, 기차, 선로 에셋을 어떻게 생성, 게임 적용할지 계속 연구해보기...

3. 밤하늘, 우주를 달리는 기차라는 컨셉으로
게임 시작 전 게임 홈 화면에서 언젠가 코레일도 밤하늘을 달릴 수 있을까요? 이런 상상력 자극하고 재미있는 감성 멘트 넣기?
광명역 도착 이런것 배경에 안어울리는 건 뺼 수도 있고

4. 게임 홈화면, 게임 오버시 뜨는 화면, 리더보드 등 ui,ux 개선

  탄소리포트
  1. 산정근거 올바른지, 산정근거에 따른 탄소 감축량 표현이 ui상 올바르게 나오는지 확인.

  제안서
  1. 제안서 뽑아서 문구 작성 끝마치기



  ====2026.09.22 15:23========

  1. 게임 홈화면이 밋밋한 느낌.
  홈화면에도 특정 애니메이션 효과 줘서 심심사힞 않게 만들기.
  ex-애니팡에서 원숭이가 광선총 쓰면서 둥둥 떠다늬는 이미지 처럼 동적 효과 줘야 안 밋밋하고 재밌을 듯.

  2. 사운드 작업
 - 사운드 컨셉 여러 개 뽑고 서정성 + 아케이드 게임 bgm컨셉 등 컨셉부터 확실히 정하기.
 - 게임 홈화면 bgm
 - play, leaderboard, settings등 버튼 터치 효과음
 - 게임 bgm
 - 부스터 상태의 bgm
 - 좌우 이동시 터치음 좌우 터치에 따라 왼쪽에서만 또는 오른쪽에서만 소리 나는 것도 좋을 듯.
 
 3. 이미지 전면 리셋?
- 현재 너무 sf적인 이미지
- 레일은 탑뷰인데 기차는 묘하게 사선에서 보고 있고
- 배경은 밤하늘이라서 어디를 달리는지 불명확함.
- 컨셉과 비주얼 디자인 면확화 필요

4. 게임 배경 선로, 기차 탑뷰인 건 좋음. 
실제 무한의 계단 게임은 캐릭터가 계단 올라가면서 배경 화면이 지상에서 점점 고층 빌딩 드으로 옮겨가는 과정이 있음.
이런 배경 변화에서 오는 재미가 있는 것 같은데
나는 배경 자체가 png로 고정되어 있기 떄문에 이런 배경을 느끼기 힘듬.